# PostgreSQL datasource — restore in prisma/schema.prisma when done with local SQLite.
#
# datasource db {
#   provider = "postgresql"
#   url      = env("DATABASE_URL")
# }

provider = "postgresql"
url      = env("DATABASE_URL")
