// The shared public demo account.
//
// Its address is the one thing /api/demo-login and the AI entitlement check must
// agree on, so it lives here rather than being spelled out in both.
export const DEMO_EMAIL = 'demo@studypetplus.app';
